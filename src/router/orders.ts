// import { Router } from "express";
// import { create, Delete, getAll, getOne, update } from "../controllers/orders";
// const router: Router = Router();

// router.route("/").get(getAll).post(create);
// router.route("/:id").get(getOne).patch(update).delete(Delete);

// export default router;
